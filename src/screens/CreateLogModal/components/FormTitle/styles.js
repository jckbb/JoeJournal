import {StyleSheet} from 'react-native';
import {IVORY_WHITE} from '../../../../common/res/colors';

export default StyleSheet.create({
  titleText: {
    fontSize: 30,
    fontWeight: '500',
    color: IVORY_WHITE,
  },
});
