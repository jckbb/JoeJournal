import {StyleSheet} from 'react-native';
import {PRIMARY_COLOR_800} from '../../../../res/colors';

export const iconColor = PRIMARY_COLOR_800;

export default StyleSheet.create({
  button: {
    position: 'absolute',
    top: 25,
    right: 25,
  },
});
