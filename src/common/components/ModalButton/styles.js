import {StyleSheet} from 'react-native';

import {MODAL_BUTTON_TEXT} from '../../res/themes';

export default StyleSheet.create({
  button: {
    paddingHorizontal: 20,
    paddingVertical: 10,
  },
  buttonText: MODAL_BUTTON_TEXT,
});
