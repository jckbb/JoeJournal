import {StyleSheet} from 'react-native';
import {WHITE} from '../../res/colors';

export default StyleSheet.create({
  step: {
    alignItems: 'center',
  },
  stepText: {
    color: WHITE,
    fontSize: 20,
    fontWeight: '700',
  },
});
